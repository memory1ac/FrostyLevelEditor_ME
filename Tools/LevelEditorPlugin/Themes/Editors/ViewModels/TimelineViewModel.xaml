<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:editors="clr-namespace:LevelEditorPlugin.Editors"
    xmlns:z="clr-namespace:LevelEditorPlugin.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml" />
        <ResourceDictionary Source="/LevelEditorPlugin;component/Themes/Common.xaml" />
        <ResourceDictionary Source="/LevelEditorPlugin;component/Themes/Controls/Toolbar.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate DataType="{x:Type editors:TimelineViewModel}">
        <ctrl:FrostyDockablePanel
            editors:LevelEditorAttachedBehaviors.HideCommand="{Binding Path=HideCommand}"
            editors:LevelEditorAttachedBehaviors.LoadedCommand="{Binding Path=InitializeCommand}"
            editors:LevelEditorAttachedBehaviors.ShowCommand="{Binding Path=ShowCommand}"
            editors:LevelEditorAttachedBehaviors.UnloadedCommand="{Binding Path=UnloadCommand}"
            BorderVisible="False"
            TitleBarVisible="False">

            <Border BorderBrush="{StaticResource ControlBackground}" BorderThickness="0,2,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="toolbarRow" Height="26" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <z:Toolbar Grid.Row="0" ItemsSource="{Binding Path=ToolbarItems}" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="leftColumn" Width="{Binding Path=Size, ElementName=timelineLeftDockPanel, Mode=TwoWay}" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition x:Name="rightColumn" Width="{Binding Path=Size, ElementName=timelineRightDockPanel, Mode=TwoWay}" />
                        </Grid.ColumnDefinitions>

                        <Border
                            x:Name="leftContent"
                            Grid.Column="0"
                            Background="{StaticResource ControlBackground}">
                            <Grid Margin="2" Background="{StaticResource ListBackground}">

                                <z:DockPanel
                                    x:Name="timelineLeftDockPanel"
                                    DockLocation="Left"
                                    DockManager="{Binding Path=DockManager}" />

                            </Grid>
                        </Border>

                        <GridSplitter
                            x:Name="leftGridSplitter"
                            Grid.Column="1"
                            Width="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Stretch"
                            Background="{StaticResource ControlBackground}"
                            ResizeDirection="Columns"
                            ShowsPreview="True" />

                        <ctrl:FrostyDockablePanel
                            x:Name="mainContent"
                            Title=""
                            Grid.Column="2"
                            BorderVisible="False"
                            TitleBarVisible="False">

                            <Border
                                x:Name="mainBorder"
                                BorderBrush="{StaticResource ControlBackground}"
                                BorderThickness="0,2,0,2">
                                <ScrollViewer>
                                    <Grid Background="{StaticResource ListBackground}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="4" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="8*" />
                                        </Grid.ColumnDefinitions>

                                        <TreeView
                                            Grid.Column="1"
                                            ItemsSource="{Binding Path=Tracks}"
                                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                            VirtualizingPanel.IsVirtualizing="True">
                                            <TreeView.Template>
                                                <ControlTemplate>
                                                    <Border
                                                        Padding="4"
                                                        BorderBrush="#00000000"
                                                        BorderThickness="0"
                                                        SnapsToDevicePixels="true">
                                                        <ItemsPresenter />
                                                    </Border>
                                                </ControlTemplate>
                                            </TreeView.Template>
                                            <TreeView.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <VirtualizingStackPanel />
                                                </ItemsPanelTemplate>
                                            </TreeView.ItemsPanel>
                                            <TreeView.ItemContainerStyle>
                                                <Style BasedOn="{StaticResource StrechedTreeViewItemStyle}" TargetType="{x:Type TreeViewItem}">
                                                    <Setter Property="Foreground" Value="{StaticResource FontColor}" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="TreeViewItem.IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay}" />
                                                    <Setter Property="TreeViewItem.IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay}" />
                                                </Style>
                                            </TreeView.ItemContainerStyle>
                                            <TreeView.ItemTemplate>
                                                <HierarchicalDataTemplate ItemsSource="{Binding Path=Tracks}">
                                                    <StackPanel x:Name="container" Orientation="Horizontal">
                                                        <Image
                                                            x:Name="icon"
                                                            Width="16"
                                                            Margin="0,0,8,0"
                                                            Source="{Binding Path=Icon}" />
                                                        <TextBlock
                                                            VerticalAlignment="Center"
                                                            Text="{Binding Path=TrackName}"
                                                            ToolTip="{Binding Path=ToolTip}" />
                                                    </StackPanel>
                                                    <HierarchicalDataTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding Path=Icon}" Value="{x:Null}">
                                                            <Setter TargetName="icon" Property="Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding Path=IsEntityTrack}" Value="True" />
                                                                <Condition Binding="{Binding Path=IsSelected}" Value="True" />
                                                            </MultiDataTrigger.Conditions>
                                                            <MultiDataTrigger.Setters>
                                                                <Setter TargetName="container" Property="ContextMenu">
                                                                    <Setter.Value>
                                                                        <ContextMenu>
                                                                            <MenuItem Command="{Binding Path=SelectEntityInSceneCommand}" Header="Select Entity in Scene" />
                                                                        </ContextMenu>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </MultiDataTrigger.Setters>
                                                        </MultiDataTrigger>
                                                    </HierarchicalDataTemplate.Triggers>
                                                </HierarchicalDataTemplate>
                                            </TreeView.ItemTemplate>
                                        </TreeView>

                                        <GridSplitter
                                            Grid.Column="2"
                                            Width="3"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Stretch"
                                            Background="{StaticResource ControlBackground}"
                                            ResizeDirection="Columns" />

                                        <TreeView
                                            Grid.Column="3"
                                            ItemsSource="{Binding Path=DataTracks}"
                                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                            VirtualizingStackPanel.IsVirtualizing="True">
                                            <TreeView.Template>
                                                <ControlTemplate>
                                                    <Border
                                                        Padding="4"
                                                        Background="{StaticResource WindowBackground}"
                                                        BorderBrush="#00000000"
                                                        BorderThickness="0"
                                                        SnapsToDevicePixels="true">
                                                        <ItemsPresenter />
                                                    </Border>
                                                </ControlTemplate>
                                            </TreeView.Template>
                                            <TreeView.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <VirtualizingStackPanel />
                                                </ItemsPanelTemplate>
                                            </TreeView.ItemsPanel>
                                            <TreeView.ItemContainerStyle>
                                                <Style BasedOn="{StaticResource ThemelessTreeViewItemStyle}" TargetType="{x:Type TreeViewItem}">
                                                    <Setter Property="Foreground" Value="{StaticResource FontColor}" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="TreeViewItem.IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay}" />
                                                    <Setter Property="TreeViewItem.IsSelected" Value="{Binding Path=IsSelected, Mode=TwoWay}" />
                                                </Style>
                                            </TreeView.ItemContainerStyle>
                                            <TreeView.ItemTemplate>
                                                <HierarchicalDataTemplate ItemsSource="{Binding Path=DataTracks}">
                                                    <ContentPresenter Content="{Binding Path=TrackData}" />
                                                </HierarchicalDataTemplate>
                                            </TreeView.ItemTemplate>
                                        </TreeView>

                                        <z:TrackScrubberControl
                                            Grid.Column="3"
                                            CurrentTime="{Binding Path=PlaybackTime}"
                                            EndTime="{Binding Path=EndTime}"
                                            StartTime="{Binding Path=StartTime}" />

                                        <Border
                                            Grid.Column="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            Background="{StaticResource ListBackground}" />

                                    </Grid>
                                </ScrollViewer>
                            </Border>
                        </ctrl:FrostyDockablePanel>

                        <GridSplitter
                            x:Name="rightGridSplitter"
                            Grid.Column="3"
                            Width="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Stretch"
                            Background="{StaticResource ControlBackground}"
                            ResizeDirection="Columns"
                            ShowsPreview="True" />

                        <Border
                            x:Name="rightContent"
                            Grid.Column="4"
                            Background="{StaticResource ControlBackground}">
                            <Grid Margin="2" Background="{StaticResource ListBackground}">

                                <z:DockPanel
                                    x:Name="timelineRightDockPanel"
                                    DockLocation="Right"
                                    DockManager="{Binding Path=DockManager}" />

                            </Grid>
                        </Border>

                    </Grid>

                </Grid>
            </Border>
        </ctrl:FrostyDockablePanel>

        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding ElementName=timelineLeftDockPanel, Path=HasItems}" Value="False">
                <Setter TargetName="leftContent" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="leftGridSplitter" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="mainContent" Property="Grid.Column" Value="0" />
                <Setter TargetName="mainContent" Property="Grid.ColumnSpan" Value="3" />
                <Setter TargetName="mainBorder" Property="BorderThickness" Value="2,2,0,2" />
            </DataTrigger>
            <DataTrigger Binding="{Binding ElementName=timelineRightDockPanel, Path=HasItems}" Value="False">
                <Setter TargetName="rightContent" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="rightGridSplitter" Property="Visibility" Value="Collapsed" />
                <Setter TargetName="mainContent" Property="Grid.ColumnSpan" Value="3" />
                <Setter TargetName="mainBorder" Property="BorderThickness" Value="0,2,2,2" />
            </DataTrigger>
            <MultiDataTrigger>
                <MultiDataTrigger.Conditions>
                    <Condition Binding="{Binding ElementName=timelineLeftDockPanel, Path=HasItems}" Value="False" />
                    <Condition Binding="{Binding ElementName=timelineRightDockPanel, Path=HasItems}" Value="False" />
                </MultiDataTrigger.Conditions>
                <MultiDataTrigger.Setters>
                    <Setter TargetName="leftContent" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="leftGridSplitter" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="rightContent" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="rightGridSplitter" Property="Visibility" Value="Collapsed" />
                    <Setter TargetName="mainContent" Property="Grid.Column" Value="0" />
                    <Setter TargetName="mainContent" Property="Grid.ColumnSpan" Value="5" />
                    <Setter TargetName="mainBorder" Property="BorderThickness" Value="2,2,2,2" />
                </MultiDataTrigger.Setters>
            </MultiDataTrigger>
        </DataTemplate.Triggers>

    </DataTemplate>

</ResourceDictionary>