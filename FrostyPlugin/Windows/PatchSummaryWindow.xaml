<ctrl:FrostyDockableWindow
    x:Class="Frosty.Core.Windows.PatchSummaryWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:Frosty.Core.Converters"
    xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Patch Summary"
    Width="600"
    Height="527"
    ResizeMode="NoResize"
    SnapsToDevicePixels="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <ctrl:FrostyDockableWindow.Resources>
        <conv:StringToBitmapSourceConverter x:Key="StringToBitmapSourceConverter" />
        <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="0" />
        </Style>
    </ctrl:FrostyDockableWindow.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition />
            <RowDefinition Height="38" />
        </Grid.RowDefinitions>

        <Grid Background="{StaticResource ListBackground}">
            <TextBlock
                Margin="3"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                DockPanel.Dock="Bottom"
                FontFamily="Global User Interface"
                FontSize="16"
                Foreground="{StaticResource FontColor}"
                Text="Game was recently updated. Here is a summary of the changes." />
        </Grid>

        <Grid Grid.Row="1" Margin="2">
            <TabControl
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                SelectionChanged="TabControl_SelectionChanged">
                <TabItem Header="Assets Added" IsSelected="True">
                    <Border
                        Background="{StaticResource WindowBackground}"
                        BorderBrush="#454545"
                        BorderThickness="2">
                        <Grid>
                            <ListBox x:Name="addedAssetsList">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Height="22" Orientation="Horizontal">
                                            <Image
                                                x:Name="imageRect"
                                                Grid.Column="0"
                                                Width="16"
                                                Height="16"
                                                Source="{Binding Path=Type, Converter={StaticResource StringToBitmapSourceConverter}}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Text="{Binding Path=DisplayName}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Opacity="0.5">
                                                <TextBlock.Text>
                                                    <Binding Path="Name" StringFormat="({0})" />
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </TabItem>
                <TabItem Header="Assets Modified">
                    <Border
                        Background="{StaticResource WindowBackground}"
                        BorderBrush="#454545"
                        BorderThickness="2">
                        <Grid>
                            <ListBox x:Name="modifiedAssetsList">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Height="22" Orientation="Horizontal">
                                            <Image
                                                x:Name="imageRect"
                                                Grid.Column="0"
                                                Width="16"
                                                Height="16"
                                                Source="{Binding Path=Type, Converter={StaticResource StringToBitmapSourceConverter}}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Text="{Binding Path=DisplayName}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Opacity="0.5">
                                                <TextBlock.Text>
                                                    <Binding Path="Name" StringFormat="({0})" />
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </TabItem>
                <TabItem Header="Assets Removed">
                    <Border
                        Background="{StaticResource WindowBackground}"
                        BorderBrush="#454545"
                        BorderThickness="2">
                        <Grid>
                            <ListBox x:Name="removedAssetsList">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Height="22" Orientation="Horizontal">
                                            <Image
                                                x:Name="imageRect"
                                                Grid.Column="0"
                                                Width="16"
                                                Height="16"
                                                Source="{Binding Path=Type, Converter={StaticResource StringToBitmapSourceConverter}}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Text="{Binding Path=DisplayName}" />
                                            <TextBlock
                                                Margin="6,0,0,0"
                                                VerticalAlignment="Center"
                                                FontFamily="MS Reference Sans Serif"
                                                FontWeight="Normal"
                                                Opacity="0.5">
                                                <TextBlock.Text>
                                                    <Binding Path="Name" StringFormat="({0})" />
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </TabItem>
            </TabControl>

            <TextBlock
                x:Name="totalTextBlock"
                Margin="0,3,6,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Foreground="{StaticResource FontColor}"
                TextAlignment="Right" />
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource ListBackground}">
            <Grid Margin="8">
                <DockPanel LastChildFill="False">
                    <Button
                        x:Name="exportButton"
                        Width="75"
                        Click="exportButton_Click"
                        Content="Export"
                        DockPanel.Dock="Left" />
                    <Button
                        x:Name="okayButton"
                        Width="75"
                        Click="okayButton_Click"
                        Content="Ok"
                        DockPanel.Dock="Right" />
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
